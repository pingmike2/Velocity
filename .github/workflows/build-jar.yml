name: Build and Release Velocity

on:
  push:
    branches: [ main ]
    paths:
      - 'proxy/src/main/java/com/velocitypowered/proxy/Velocity.java'
  workflow_dispatch:

permissions:
  contents: write 

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      jar_name: ${{ steps.prepare_jar.outputs.jar_name }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build with Gradle
        run: chmod +x gradlew && ./gradlew clean shadowJar

      - name: Prepare JAR
        id: prepare_jar
        run: |
          JAR_NAME="velocity.jar"
          JAR_PATH=$(find proxy/build/libs -name "*.jar" ! -name "*sources*" ! -name "*javadoc*" | head -n 1)
          cp "$JAR_PATH" "./$JAR_NAME"
          echo "jar_name=$JAR_NAME" >> $GITHUB_OUTPUT
        shell: bash

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: velocity-artifact
          path: "${{ steps.prepare_jar.outputs.jar_name }}"

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: velocity-artifact

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: "${{ needs.build.outputs.jar_name }}"
          tag_name: ${{ github.ref_name }}
          name: "Velocity ${{ github.ref_name }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}